package lesson4_interface;

public interface Bank {
	void getInterestRate();
	// All methods inside the interface are abstract by nature.
	// An Intercace only can be implemented and not extended
	// Keyword "implements" is used instead of "extends"
}
